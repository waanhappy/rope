### commonDataWebPlugin 给 web 用的 plugin

``` javascript
commonDataWebPlugin({
  initialData: { collapsed: { enable: true } },
  configs: { items: [{ user: { url: 'xxxxx', excludePath: [], options: {}, } }] },
}, history)
```